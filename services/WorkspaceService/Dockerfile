FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

# Copy the project file and restore dependencies
# Note: Ensure your .csproj is named WorkspaceService.csproj or update the names below
COPY ["WorkspaceService.csproj", "./"]
RUN dotnet restore "WorkspaceService.csproj"

COPY . .
RUN dotnet build "WorkspaceService.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "WorkspaceService.csproj" -c Release -o /app/publish

FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS final
WORKDIR /app
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "WorkspaceService.dll"]